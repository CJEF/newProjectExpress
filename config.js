module.exports = {
    PORT: process.env.PORT || 3000,
    MONGO_URL: 'mongodb://localhost/expressLessons' // скачать mongo db на пк и в командой строке с помощью команды use <namebd> создаем бд, далее заходим в компасс mongo и вводим данную строку в запросившийся юрл
}